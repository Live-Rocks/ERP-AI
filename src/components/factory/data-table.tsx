import { Inbox } from "lucide-react";
export interface DataTableColumn<T> { key: string; header: string; className?: string; render: (row: T) => React.ReactNode; }
export function DataTable<T extends { id: string }>({ caption, columns, rows, emptyMessage = "目前沒有可顯示的資料。" }: { caption: string; columns: DataTableColumn<T>[]; rows: T[]; emptyMessage?: string }) {
  return <div className="overflow-hidden rounded-xl border border-border bg-card"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><caption className="sr-only">{caption}</caption><thead className="border-b border-border bg-muted/55 text-xs font-medium text-muted-foreground"><tr>{columns.map((column) => <th key={column.key} className={`whitespace-nowrap px-4 py-3 font-medium ${column.className ?? ""}`}>{column.header}</th>)}</tr></thead><tbody className="divide-y divide-border">{rows.map((row) => <tr key={row.id} className="hover:bg-muted/30">{columns.map((column) => <td key={column.key} className={`px-4 py-3 ${column.className ?? ""}`}>{column.render(row)}</td>)}</tr>)}</tbody></table></div>{rows.length === 0 && <div className="flex min-h-32 flex-col items-center justify-center gap-2 border-t border-border p-5 text-center text-sm text-muted-foreground"><Inbox aria-hidden="true" className="size-5" />{emptyMessage}</div>}</div>;
}
